"use strict"; // treat all JS code as newer version

let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => range (2 to power 53). It can be an integer or a floating-point number.
// bigint
// string => " "
// boolean => true/false
// null => standalone value (Represents the intentional absence of any object value.)
// undefined => Represents a variable that has been declared but has not been assigned a value.

// symbol => A unique and immutable primitive value. Symbols are often used as unique identifiers for object properties.

 const mySymbol = Symbol("description");

// object => Represents a collection of key-value pairs. Each key is a string, and values can be of any data type.


console.log(typeof undefined); // undefined
console.log(typeof null); // object

// 'typeof' is a keyword for getting the datatype.